[project]
name = "intertext-ingest"
version = "0.1.0"
description = "Intertext data ingestion package"
requires-python = ">=3.12,<3.13"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["intertext_ingest"]

