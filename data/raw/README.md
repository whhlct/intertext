# Raw source artifacts

The ingestion pipeline stores downloaded archives, source checkouts, and acquisition metadata here by default. These files are intentionally ignored by Git; authoritative source URLs, revisions, checksums, retrieval times, and licenses are also recorded on each database release.
